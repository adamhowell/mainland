import React from "react";
import styles from "./Canvas.module.scss"

const Canvas = () => {
  return <div className={`${styles.root} bg-secondary text-white`}>Canvas</div>;
};

export default Canvas;