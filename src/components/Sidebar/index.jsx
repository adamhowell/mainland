import React from "react";
import styles from "./Sidebar.module.scss"

const Sidebar = () => {
  return <div className={`${styles.root} bg-primary text-white`}>Sidebar</div>;
};

export default Sidebar;